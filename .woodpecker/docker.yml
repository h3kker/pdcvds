when:
  - event: push
    branch: [master,sqlize]
steps:
- name: bauhaus
  image: woodpeckerci/plugin-docker-buildx
  settings:
      auto_tag: true
      purge: false
      repo: repo.testha.se/hekker/pdcvds
      tags: latest
      password:
        from_secret: pkg push
      username: hekker
      registry: repo.testha.se